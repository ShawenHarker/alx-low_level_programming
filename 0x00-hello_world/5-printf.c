#include <stdio.h>

/**
 *
 * main -Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char string[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%c\n", string);
	return (0);
}
