#include "main.h"

/**
 * print_most_numbers - Prints the numbers, from 0 to 9 without 2 and 4.
 * Return: Nothing.
 */

void print_most_numbers(void)
{
	int ch;

	if (ch != 2 && ch != 4)
		_putchar(ch);
	_putchar('\n');
}
